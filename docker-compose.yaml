version: '3'
services:
  backend:
    build:
      context: ./
      dockerfile: backend/Dockerfile
    image: wozniakslawomir/slawomir-wozniak-backend:latest
    ports:
      - 8000:8000
    volumes:
      - ./backend:/app